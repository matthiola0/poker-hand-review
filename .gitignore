# hand histories (personal data, not version-controlled) — keep only the sample template in data/
*.txt
data/*
!data/sample.txt
!data/README.md

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# exports / generated reports (large, not version-controlled)
out.json
report.json
report.*.json
.gstack/

# design doc (private, not uploaded)
docs/SDD.md

# local personal settings (not version-controlled)
.claude/settings.local.json

# gto-preflop: local-only source material (not uploaded).
# Only the generated charts JSON (under each set's charts/) is tracked.
gto-preflop/**/*.png
gto-preflop/**/*.md

# Local-only chart-building helper (not part of the published package).
tools/extract_preflop_charts.py
