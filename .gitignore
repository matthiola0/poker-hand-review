# hand histories (personal data, not version-controlled) — keep only the sample template in data/
*.txt
data/*
!data/sample.txt
!data/README.md

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# exports / generated reports (large, not version-controlled)
out.json
report.json
report.*.json
.gstack/

# design doc (private, not uploaded)
docs/SDD.md

# local personal settings (not version-controlled)
.claude/settings.local.json
