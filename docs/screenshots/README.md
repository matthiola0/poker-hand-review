# Screenshots

README screenshots live here.

Recommended: **PNG, width ≥ 1200px** (the README displays them downscaled). Match the filenames below and the README picks them up automatically.

Files currently used by the README:

| Filename | What it shows |
|---|---|
| `hero.png` | Full UI overview (top hero image) |
| `hand-list.png` | Hand list panel |
| `hand-replay.png` | Hand replay + decision card |
| `leaks.png` | Leaks panel |
| `positions.png` | Net-by-position bars |

Optional, not yet added:

| Filename | What to capture |
|---|---|
| `opponent-profile.png` | Opponent profile panel (right side; may need a toggle/scroll) |

Tip: load a real report first (`n8-review web --report report.json`) so the screens have content.
